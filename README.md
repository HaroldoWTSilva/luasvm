# luasvm

Support Vector Machine implementation in Lua.

Specifically, it's the implementation of the Sequential Minimal Optimization
(SMO) algorithm, WSS3 variant.

Running `luajit main.lua` should train and test a SVM.
